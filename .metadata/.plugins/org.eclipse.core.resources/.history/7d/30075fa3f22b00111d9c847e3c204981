package com;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class Main {

	public static void main(String[] agr) {
		List<String> str = Arrays.asList("a", "b", "c", "d", "e");

		List<Integer> list = Arrays.asList( 7, 8, 10,3, 4, 5, 1, 2, 6);
		
		System.out.print(
		list.stream().map(i -> i*10).filter(i -> i%2==0).collect(Collectors.toList()));
		
		
		
		
		

	}

}
